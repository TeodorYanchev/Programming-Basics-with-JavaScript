function sumFirstAndLastArrayElements(data) {
    let firstNumber = data[0];
    let lastElementIndex = data.length - 1;
    let lastNumber = data[lastElementIndex];

    console.log(firstNumber + lastNumber);
}